export interface YutInfo {
  /**
   * @desc
   * 윷가락이 뒤집어져 있는지 여부. 뒤집어져 있으면 true, 펴져 있으면 false.
   */
  isFlipped: boolean;
  /**
   * @desc
   * 빽도인지 판별하기 위한 특수 윷가락인지 여부.
   */
  isMarked: boolean;
}
